# Exact versions pinned for reproducibility — do not upgrade without testing.
# Critical constraints:
#   numpy < 2       — PyTorch 2.2.2 is incompatible with NumPy 2.x
#   transformers == 4.40.0 — newer versions require torch >= 2.4
#   shap == 0.43.0  — newer versions require numpy >= 2
#   Python == 3.11  — PyTorch 2.2.2 is not available for 3.12+

# Core data science
pandas==3.0.2
numpy==1.26.4
scikit-learn==1.8.0
scipy==1.17.1

# NLP
nltk==3.9.4

# Deep learning
torch==2.2.2
transformers==4.40.0

# Explainability
shap==0.43.0
lime==0.2.0.1

# Visualization
matplotlib==3.10.8
seaborn==0.13.2
wordcloud==1.9.6

# Utilities
tqdm==4.67.3
openpyxl==3.1.5
